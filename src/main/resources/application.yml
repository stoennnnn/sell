spring:
  datasource:
    jdbc.drive: com.mysql.cj.jdbc.Driver
    username: root
    password: qaz123456
    url: jdbc:mysql://localhost:3306/sell?characterEncoding=utf-8&serverTimezone=UTC&useSSL=false
  jpa:
      show-sql: true
#返回前端不显示null字段
  jackson:
    default-property-inclusion: non_null
server:
  context-path: /sell